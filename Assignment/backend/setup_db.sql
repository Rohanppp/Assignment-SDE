-- SQL script to create the database if it doesn't exist
-- Run this in psql or pgAdmin after installing Postgres

-- Connect to postgres database first, then run:
CREATE DATABASE reviews;

-- Or if the database already exists, this script is safe to run multiple times

